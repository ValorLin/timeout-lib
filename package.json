{
  "name": "timeout-lib",
  "version": "0.0.3",
  "main": "lib/index.js",
  "module": "dist/timeout-lib.esm.js",
  "browser": "dist/timeout-lib.umd.js",
  "author": "Valor Lin",
  "keywords": [
    "typescript",
    "timeout",
    "async"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^25.1.2",
    "jest": "^25.1.0",
    "rollup": "^1.0.0",
    "rollup-plugin-typescript": "^1.0.0",
    "ts-jest": "^25.2.0",
    "tslib": "^1.10.0",
    "typescript": "^3.2.2"
  },
  "scripts": {
    "prebuild": "rm -fr lib dist",
    "build": "rollup -c && tsc --project tsconfig.build.json",
    "prepublishOnly": "npm run build",
    "test": "jest src",
    "dev": "jest --watch --only-changed"
  },
  "files": [
    "dist",
    "lib"
  ]
}
